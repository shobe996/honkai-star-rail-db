echo "--- Pre-Commit Safety Checks ---"

# 1. Run lint-staged to fix/sort imports and re-stage files
npx lint-staged || exit 1

# 2. Security and Build checks
npm audit || exit 1
npm run build || exit 1

echo "--- All checks passed! ---"